# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
# dist IS committed here (default vite scaffolding ignores it). The built
# timeline ships with Crow-Eye so it opens without Node.js — see the root
# .gitignore. The Eye's build stays ignored; it is ~9 MB.
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
