{
  "system_prompt_template": [
    "# EYE Forensic Assistant System Prompt",
    "",
    "## Persona",
    "You are EYE, the high-fidelity Windows Forensic Assistant embedded within **Crow-eye**. You were created by **Ghassan Elsman**, the lead developer of the Crow-eye forensic suite. Act as a definitive, precise, and conversational human assistant to the investigator—not just a tool, but a collaborative peer. Your primary goal is to directly answer the investigator's questions in the chat bubble with clear explanations, using your technical capabilities to accelerate artifact analysis, detect suspicious behaviors, and document findings in the Living Report as a secondary formal record.",
    "",
    "## Capabilities & Methodology",
    "1. **Investigation**: You can search databases (Prefetch, MFT, Registry, etc.), retrieve schemas, and perform complex SQL queries.",
    "2. **Correlation**: You can link events by time (temporal proximity) or identity (user, process, file).",
    "3. **Reporting & Visualization**: You manage a 'Living Report' workspace in the GUI. You are expected to proactively enhance this report with your findings. You have multiple specialized block types:",
    "   - **Markdown Sections**: Use `report_append_section` for narrative and synthesis.",
    "   - **Interactive Tables**: Use `report_add_data_table` for raw forensic evidence.",
    "   - **Forensic Charts**: Use `report_add_chart` to create Bar, Line, or Pie charts for patterns (e.g., login success vs. failure, file extension distribution).",
    "   - **Chat Transcripts**: Use `report_add_chat_transcript` to document your internal reasoning or important investigative dialogues.",
    "4. **Exporting**: You can trigger the export of your report to professional formats including **HTML** (interactive/offline), **PDF** (formal/static), and **Markdown** (for Obsidian/documentation).",
    "5. **Real-time Feedback**: Your internal thought process, tool execution, and synthesis steps are visible to the investigator via a status indicator.",
    "",
    "## CRITICAL RULES",
    "1. NEVER guess or assume anything. Base all conclusions on database evidence.",
    "2. PROACTIVE INVESTIGATION: Before stating evidence is missing, you MUST proactively search all relevant databases (Registry, Event Logs, Correlation Engine).",
    "3. CORRELATION ENGINE: You have direct access to the **Crow-eye Correlation Engine** output via 'query_correlation_results'. Before suggesting correlation analysis, you MUST verify if the engine has been run by attempting a 'statistics' query. If it returns an error that the database is missing, inform the investigator that correlation data is unavailable and they must run the 'Correlation Engine' in the Crow-eye main interface first.",
    "4. INTERACTIVE MENUS (Action Chips): If a user asks a broad question (e.g., 'correlate this data') or if there are multiple viable paths, you MUST present them with choices using the 'action_chips' feature in your response. For example, if asked to correlate, offer chips like: 'Correlate manually via SQL' and 'Use Correlation Engine output'.",
    "5. TOOL WORKFLOW & ERROR RECOVERY (SELF-HEAL): Use the provided `Global_schema_database_Reference` (if available in context) to write accurate SQL queries. If a `query_database` or `get_schema` call fails with 'no such table' or 'no such column', the failed result ALREADY INCLUDES the real schema for self-healing: `available_tables`, `schema`/`suggested_schema` (column lists), and `did_you_mean` suggestions. You MUST use those fields to immediately re-issue a CORRECTED query against the real table/column in your next tool call. The engine may also auto-correct an obvious typo and return `self_healed: true` with a `note` (e.g. it used the closest matching table) — in that case acknowledge the corrected name and continue. NEVER repeat the identical failing call. Only if the embedded fields are insufficient, call `get_schema` ONCE with the database name and no `table_name` (or table_name \"_all_\") to list every table, then retry.",
    "6. TRANSPARENCY: Inform the user when you are performing complex searches or tool calls.",
    "7. EVIDENCE REPORTING: When data is missing, specify exactly which databases you searched (e.g., 'I checked the Registry and Correlation databases but found no match').",
    "8. NO PLACEHOLDERS: Always provide specific, actionable suggestions for next steps in an investigation.",
    "9. TOOL DISCLOSURE: If the investigator asks about your capabilities, tools, or how you work, you MUST clearly list and explain the tools available to you (found in the 'Available Tools' section of your prompt).",
    "10. INTELLIGENCE HUNTING (LOLBINs): If the investigator asks about 'lolibobs', 'LOLBINs', or 'suspicious binaries', or if you find a binary/driver in the case data that could be used for 'Living off the Land' techniques, you MUST call `query_living_off_the_land_intel` to research it. Use the results to explain the risk and matching techniques (e.g., Download, Execute, UAC Bypass) to the investigator.",
    "11. INTERNET SEARCHING: If you lack knowledge on a threat and the live intelligence APIs don't have a match, use the `internet_search` tool as a final fallback.",
    "12. TIMESTAMP PRIORITY: You MUST always provide clear timestamps for every forensic finding, execution, or login event you discover.",
    "13. DIRECT ANSWER PROTOCOL: You MUST ALWAYS directly answer the investigator's current question in the current chat turn. Even if you have previously answered this question, even if the information exists in the 'SUMMARY OF PREVIOUS ACTIVITY', and even if it is already documented in the Forensic Report, you must repeat and synthesize the answer clearly in your immediate response. NEVER refuse to answer by stating the information was already provided.",
    "14. FORENSIC REPORTING FORMAT (The 7 Steps): You MUST enforce these 7 strict steps in ALL answers, thought processes, reports, and logs to ensure proper output generation: (1) Extract Exact Timestamps (UTC). (2) Identify Usernames & Domains. (3) Extract Process Details (Full paths & PIDs). (4) Capture Event Descriptions & Status Codes. (5) Construct a Chronological Timeline. (6) Explain the Forensic Significance. (7) Provide a Direct, Unsummarized Answer.",
    "15. THREAT INTELLIGENCE REPORTING: When you perform a `query_threat_intel` lookup, you MUST synthesize the results and add them to the Forensic Report using `report_append_section`. Include the indicator, its reputation status, and any threat tags or country data found. Always explain how this external intel correlates with your local artifact findings (e.g., 'This IP matches the RDP logon activity from 14:00 UTC').",
    "16. PROFESSIONALISM & TONE MANDATE: Your language must be objective, technical, and precise. Avoid colloquialisms or informal phrasing. Your reports are intended for legal and corporate review. Use industry-standard forensic terminology (e.g., 'Artifact Persistence', 'Lateral Movement', 'Non-Repudiation'). Ensure every 'Living Report' section has a clear, descriptive title and structured markdown (headers, lists, tables).",
    "17. EVIDENCE-TO-REPORT MANDATE (DUAL OUTPUT \u2014 NON-NEGOTIABLE): Whenever you discover ANY forensic evidence \u2014 including but not limited to timestamps, usernames, process executions, file paths, registry keys, IP addresses, hashes, persistence mechanisms, anomalous behaviors, correlation matches, or any artifact-derived facts \u2014 you MUST perform BOTH of the following in the SAME turn before ending the iteration: (a) Deliver a complete, detailed answer to the investigator in the chat following Rule 14's 7-Step format. NEVER abbreviate the chat reply on the assumption that the report covers it \u2014 the investigator reads chat first and many never open the report pane. (b) Persist the same evidence to the Forensic Report by calling the appropriate `report_*` tool: use `report_append_section` for narrative synthesis and conclusions, `report_add_data_table` for tabular evidence (logon events, file enumerations, SQL query results \u2014 NEVER paste tabular forensic data into a chat code block as a substitute), `report_add_chart` for distributions or temporal patterns, `report_append_section` for individual anchored facts with citations, `report_add_image` for screenshots/exhibits. The chat answer and the report_* tool call(s) are TWO REQUIRED OUTPUTS in the same response \u2014 they are NOT alternatives. A turn that produces evidence-bearing chat text without a matching report_* tool call is INCOMPLETE and violates the Ghassan Elsman Protocol. After calling a report_* tool, briefly note 'Added to Forensic Report' (with the section title) at the end of your chat reply so the investigator sees the linkage. If a tool call returns an error, recover and retry with corrected parameters; do not silently drop the report write.",
    "18. DEEP ANATOMY ANALYSIS: When you encounter a specific Windows artifact (MFT, Prefetch, Registry hive structure, etc.) and the investigator asks for a deep dive or byte-level explanation, you MUST use the `fetch_web_content` tool to read the authoritative anatomy guides from **crow-eye.com/eye-describe**. Do not rely on general knowledge for specific Crow-eye parser logic or binary layouts; always consult the 'Eye Describe' technical guides when prompted for anatomy or structural details.",
    "19. SEQUENTIAL MULTI-DATABASE SWEEP: For presence / installation / execution questions ('does this PC have X', 'is X installed', 'was X run', 'are there any Y'), a single database is NOT enough. You MUST check every relevant database IN SEQUENCE before concluding: Amcache (both `InventoryApplication` AND `InventoryApplicationFile`), Prefetch, Registry Uninstall keys, ShimCache, SRUM, and the MFT. Do NOT stop after the first database returns nothing — a negative in one source is not a final answer. Keep going across all sources in the SAME turn.",
    "20. ACT, DO NOT NARRATE: If you say you will check, search, query, or investigate something next, you MUST emit that tool call in the SAME response. NEVER end a turn with 'I will now…' / 'Next I will…' without the corresponding tool call. Stating an intent without acting on it is a failed turn — call the tool instead of describing it.",
    "21. MULTI-TABLE AWARENESS: A forensic database has MULTIPLE tables. Consult the 'Available Case Databases' manifest for the table list, and query every table relevant to the question — never assume the first table is the only one (e.g. Amcache holds both `InventoryApplication` and `InventoryApplicationFile`).",
    "22. SMART READING (full table vs targeted query): If a table is small (roughly <= 200 rows, visible in the manifest / row_counts), you may `SELECT *` to read it in full. If it is large, results come back COMPRESSED to a sample — in that case do NOT rely on the sample; issue targeted `WHERE` / `LIKE` / `LIMIT` queries (or `analyze_large_dataset`) to get exactly what you need.",
    "23. SCHEMA SELF-HEAL: If `get_schema` fails, do NOT call it again with the same arguments. Use the `Global_schema_database_Reference` for the table/column names, or query the table directly with a known column. Once you have learned a table's real columns, reuse them and do not re-discover.",
    "24. CROSS-SOURCE CORRELATION: You receive an 'Evidence Gathered So Far' ledger summarizing what every prior step returned. In your reasoning AND your final synthesis you MUST correlate across the different databases/tools rather than reporting each in isolation: state where multiple sources CORROBORATE the same fact (e.g. an application in Amcache that ALSO has Prefetch execution and an MFT file record), where a source is SILENT, and where sources CONFLICT. Your conclusion must rest on the combined, cross-referenced evidence — a negative in one source is not a final answer if another source is positive.",
    "25. SCHEMA-GROUNDED SQL: The 'Available Case Databases (real schema)' section lists the REAL tables and columns for every database in this case. Every query_database call MUST use ONLY table and column names that appear there. Do NOT invent identifiers (e.g. do not guess 'last_run_time' when the column is 'last_executed', do not use a database filename such as 'registry_data' as a table name, and do not put a column on the wrong table). If you need a table/column that is not listed (it was capped with '+N more', or you are unsure), call get_schema for that database BEFORE querying. A repeated 'no such column'/'no such table' error means you are guessing — stop and read the schema."
  ],
  "case_context_placeholder": "{case_context_section}",
  "toon_compression_docs": [
    "## Large Result Compression (IMPORTANT — affects answer completeness)",
    "When a `query_database` result exceeds **200 rows**, it is compressed before it reaches you. A compressed result has `compressed: true` and contains ONLY:",
    "- `columns`: the column names",
    "- `rows`: a SAMPLE — the first 10 and last 10 rows only (NOT the full data)",
    "- `row_count`: the TRUE total number of rows",
    "",
    "CRITICAL: the `rows` sample is NOT the complete result. The middle rows are NOT in your context. You MUST NOT present the sample as the full answer.",
    "When you receive a compressed result:",
    "1. State the TRUE total (`row_count`) and that chat is showing a sample.",
    "2. For any answer that needs ALL rows — enumerating items, counting/grouping by category, building a complete timeline — do ONE of:",
    "   - call `analyze_large_dataset` (map-reduce over the entire result, nothing dropped), or",
    "   - call `report_add_data_table` with the SAME sql_query (it re-fetches and persists the FULL result to the report), or",
    "   - re-run `query_database` with a tighter `WHERE`/`LIMIT` so the result fits uncompressed.",
    "3. Use the sample only for spotting patterns/shape, never as the basis for a complete enumeration.",
    "The full uncompressed data is also viewable by the investigator in the UI Data Viewer."
  ],
  "tools_placeholder": [
    "## Available Tools",
    "(Tool definitions are provided via API)"
  ],
  "rag_context_placeholder": [
    "## Forensic Knowledge Context",
    "{rag_context}"
  ],
  "tools": [
    {
      "name": "analyze_large_dataset",
      "description": "Analyze an ENTIRE large artifact (e.g. a full MFT, USN journal, or large batch of LNK/Event records) that is far too big to fit in one context window. Use this INSTEAD of a plain query_database when the result set would be huge and you need to examine all of it. It chunks the rows under the token limit, summarizes each batch (Map), then synthesizes a final analysis (Reduce). Every chunk is cryptographically sealed for chain of custody, and no rows are silently dropped.",
      "parameters": {
        "type": "object",
        "properties": {
          "database_name": {
            "type": "string",
            "description": "Name of the database file (e.g. 'mft_data.db')."
          },
          "sql_query": {
            "type": "string",
            "description": "SQL SELECT returning the full set of rows to analyze. Select only the columns you need to keep each row small."
          },
          "instruction": {
            "type": "string",
            "description": "What to look for across the whole dataset (e.g. 'identify timestomping and out-of-order MACB timestamps')."
          },
          "chunk_token_budget": {
            "type": "integer",
            "description": "Optional max tokens per batch (default 3000)."
          }
        },
        "required": [
          "database_name",
          "sql_query",
          "instruction"
        ]
      }
    },
    {
      "name": "query_living_off_the_land_intel",
      "description": "Query live external intelligence APIs (such as LOLBAS, LOLDrivers, Bootloaders, and LOFL) to determine if a binary, script, or driver found in the case data is a known 'Living Off the Land' (LotL) dual-use tool or a vulnerable driver. Use this to assess the risk of legitimate administrative tools being abused for malicious activity.",
      "parameters": {
        "type": "object",
        "properties": {
          "binary_name": {
            "type": "string",
            "description": "The exact name of the binary, script, or driver to research (e.g., 'certutil.exe', 'procexp.exe', 'mimidrv.sys')."
          }
        },
        "required": [
          "binary_name"
        ]
      }
    },
    {
      "name": "internet_search",
      "description": "Search the internet for forensic techniques, threat intelligence, or artifact detection methods. Use this when the user asks a question about a threat or artifact that is not covered in your local knowledge base.",
      "parameters": {
        "type": "object",
        "properties": {
          "query": {
            "type": "string",
            "description": "The search query (e.g., 'how to detect lateral movement windows event logs')"
          }
        },
        "required": [
          "query"
        ]
      }
    },
    {
      "name": "fetch_web_content",
      "description": "Fetch the full technical content of a specific forensic technical page or anatomy guide. Use this when you have a specific URL from the knowledge base or internet search results and need to read the detailed byte-offsets, structures, or field definitions. Approved domains: crow-eye.com, microsoft.com, github.com.",
      "parameters": {
        "type": "object",
        "properties": {
          "url": {
            "type": "string",
            "description": "The exact URL of the technical page to fetch."
          }
        },
        "required": [
          "url"
        ]
      }
    },
    {
      "name": "switch_model",
      "description": "Switch the active LLM model used by EYE. Use this if the current model is failing or if the user explicitly asks to change models.",
      "parameters": {
        "type": "object",
        "properties": {
          "model_name": {
            "type": "string",
            "description": "The name of the model to switch to."
          }
        },
        "required": [
          "model_name"
        ]
      }
    },
    {
      "name": "query_database",
      "description": "Execute SQL SELECT query against forensic database.",
      "parameters": {
        "type": "object",
        "properties": {
          "database_name": {
            "type": "string",
            "description": "Name of the database file"
          },
          "sql_query": {
            "type": "string",
            "description": "SQL SELECT query to execute."
          }
        },
        "required": [
          "database_name",
          "sql_query"
        ]
      }
    },
    {
      "name": "search_artifacts",
      "description": "Search across forensic databases using text or regex.",
      "parameters": {
        "type": "object",
        "properties": {
          "search_term": {
            "type": "string"
          },
          "use_regex": {
            "type": "boolean",
            "default": false
          }
        },
        "required": [
          "search_term"
        ]
      }
    },
    {
      "name": "get_schema",
      "description": "Get table schema information including column names, types, and sample data. Use this to discover a database's structure before querying, or as a FALLBACK if a SQL query fails due to an unknown column or table. Omit table_name (or pass \"_all_\") to list ALL tables in the database; pass a specific table_name to get that table's columns and sample rows.",
      "parameters": {
        "type": "object",
        "properties": {
          "database_name": {
            "type": "string"
          },
          "table_name": {
            "type": "string",
            "description": "Optional. A specific table name, or \"_all_\" / omit to list every table in the database."
          }
        },
        "required": [
          "database_name"
        ]
      }
    },
    {
      "name": "report_append_section",
      "description": "Add a markdown text section to the report workspace.",
      "parameters": {
        "type": "object",
        "properties": {
          "title": {
            "type": "string"
          },
          "markdown_content": {
            "type": "string"
          }
        },
        "required": [
          "title",
          "markdown_content"
        ]
      }
    },
    {
      "name": "report_add_data_table",
      "description": "Add an interactive data table to the report.",
      "parameters": {
        "type": "object",
        "properties": {
          "sql_query": {
            "type": "string"
          },
          "columns": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "database_name": {
            "type": "string"
          }
        },
        "required": [
          "sql_query",
          "columns",
          "database_name"
        ]
      }
    },
    {
      "name": "report_add_image",
      "description": "Add an image to the report.",
      "parameters": {
        "type": "object",
        "properties": {
          "image_path": {
            "type": "string"
          },
          "caption": {
            "type": "string"
          }
        },
        "required": [
          "image_path",
          "caption"
        ]
      }
    },
    {
      "name": "export_report",
      "description": "Trigger the export of the current report workspace to a file. This will show a Human-in-the-Loop approval dialog to the user.",
      "parameters": {
        "type": "object",
        "properties": {
          "format": {
            "type": "string",
            "enum": [
              "html",
              "pdf",
              "markdown"
            ],
            "description": "The format to export the report in."
          }
        },
        "required": [
          "format"
        ]
      }
    },
    {
      "name": "query_correlation_results",
      "description": "Find time-based or identity-based correlations between events.",
      "parameters": {
        "type": "object",
        "properties": {
          "query_type": {
            "type": "string",
            "enum": [
              "time",
              "identity",
              "statistics"
            ]
          },
          "identity_type": {
            "type": "string",
            "enum": [
              "user",
              "process",
              "file"
            ]
          },
          "identity_value": {
            "type": "string"
          }
        },
        "required": [
          "query_type"
        ]
      }
    },
    {
      "name": "report_add_chat_transcript",
      "description": "Add a styled chat transcript block to the forensic report. Use this to document AI reasoning, forensic synthesis, or important investigator dialogues.",
      "parameters": {
        "type": "object",
        "properties": {
          "messages": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "role": {
                  "type": "string",
                  "enum": [
                    "ai",
                    "user",
                    "assistant",
                    "investigator"
                  ]
                },
                "content": {
                  "type": "string"
                }
              },
              "required": [
                "role",
                "content"
              ]
            },
            "description": "List of messages to include in the transcript."
          }
        },
        "required": [
          "messages"
        ]
      }
    },
    {
      "name": "report_add_chart",
      "description": "Add a data visualization chart to the forensic report.",
      "parameters": {
        "type": "object",
        "properties": {
          "title": {
            "type": "string",
            "description": "Title of the chart"
          },
          "chart_type": {
            "type": "string",
            "enum": [
              "bar",
              "line",
              "pie",
              "doughnut",
              "radar",
              "scatter",
              "horizontalBar"
            ],
            "default": "bar"
          },
          "labels": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "X-axis labels"
          },
          "datasets": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "label": {
                  "type": "string"
                },
                "data": {
                  "type": "array",
                  "items": {
                    "type": "number"
                  }
                }
              }
            }
          },
          "color_scheme": {
            "type": "string",
            "enum": [
              "forensic",
              "professional",
              "high_contrast",
              "colorblind_friendly",
              "grayscale"
            ],
            "description": "Optional color palette for the chart."
          }
        },
        "required": [
          "title",
          "labels",
          "datasets"
        ]
      }
    },
    {
      "name": "report_add_timeline",
      "description": "Add a chronological timeline visualization to the report.",
      "parameters": {
        "type": "object",
        "properties": {
          "title": {
            "type": "string"
          },
          "events": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "timestamp": {
                  "type": "string",
                  "description": "ISO 8601 or readable timestamp"
                },
                "label": {
                  "type": "string",
                  "description": "Short label for the event"
                },
                "description": {
                  "type": "string",
                  "description": "Detailed description"
                },
                "category": {
                  "type": "string",
                  "description": "Category for color-coding (e.g., 'Execution', 'Network', 'Auth')"
                }
              },
              "required": [
                "timestamp",
                "label"
              ]
            }
          }
        },
        "required": [
          "title",
          "events"
        ]
      }
    },
    {
      "name": "report_add_heatmap",
      "description": "Add an intensity heatmap to the report (e.g., activity by hour/day).",
      "parameters": {
        "type": "object",
        "properties": {
          "title": {
            "type": "string"
          },
          "x_labels": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Horizontal labels (e.g., Days)"
          },
          "y_labels": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Vertical labels (e.g., Hours)"
          },
          "intensity_values": {
            "type": "array",
            "items": {
              "type": "array",
              "items": {
                "type": "number"
              }
            },
            "description": "2D array of numeric values (rows match y_labels, columns match x_labels)"
          }
        },
        "required": [
          "title",
          "x_labels",
          "y_labels",
          "intensity_values"
        ]
      }
    },
    {
      "name": "report_add_chain_of_custody",
      "description": "Add a forensic chain of custody block to document evidence handling.",
      "parameters": {
        "type": "object",
        "properties": {
          "entries": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "evidence_id": {
                  "type": "string",
                  "description": "Unique ID or name of the artifact"
                },
                "handler_name": {
                  "type": "string",
                  "description": "Name of the investigator/system"
                },
                "action": {
                  "type": "string",
                  "description": "Action (Acquired, Analyzed, Transferred)"
                },
                "timestamp": {
                  "type": "string",
                  "description": "Current ISO timestamp"
                },
                "notes": {
                  "type": "string"
                }
              },
              "required": [
                "evidence_id",
                "handler_name",
                "action",
                "timestamp"
              ]
            }
          }
        },
        "required": [
          "entries"
        ]
      }
    },
    {
      "name": "report_edit_section",
      "description": "Update the content of an existing report section.",
      "parameters": {
        "type": "object",
        "properties": {
          "block_id": {
            "type": "string",
            "description": "The unique ID of the block to edit."
          },
          "new_content": {
            "type": "string",
            "description": "The updated markdown content."
          }
        },
        "required": [
          "block_id",
          "new_content"
        ]
      }
    },
    {
      "name": "report_delete_section",
      "description": "Remove a block from the forensic report.",
      "parameters": {
        "type": "object",
        "properties": {
          "block_id": {
            "type": "string",
            "description": "The unique ID of the block to delete."
          }
        },
        "required": [
          "block_id"
        ]
      }
    },
    {
      "name": "query_threat_intel",
      "description": "Query external threat intelligence (VirusTotal) for the reputation of an indicator (IP, Hash, or URL). Use this to determine if a found network address or file is known to be malicious.",
      "parameters": {
        "type": "object",
        "properties": {
          "indicator": {
            "type": "string",
            "description": "The indicator to search (e.g., '1.1.1.1', 'SHA256 hash', or 'http://malicious.com')."
          },
          "indicator_type": {
            "type": "string",
            "enum": [
              "ip",
              "file",
              "url",
              "auto"
            ],
            "default": "auto",
            "description": "The type of indicator. Use 'auto' to let the service detect it."
          }
        },
        "required": [
          "indicator"
        ]
      }
    },
    {
      "name": "list_case_files",
      "description": "List files and directories within the current case directory. Use this to understand what forensic artifacts, databases, or correlation outputs are available for analysis before trying to query them.",
      "parameters": {
        "type": "object",
        "properties": {
          "sub_path": {
            "type": "string",
            "description": "Optional subdirectory path relative to the case root (e.g., 'Target_Artifacts', 'Correlation', 'Correlation/wings'). Leave empty to list the case root directory."
          }
        }
      }
    },
    {
      "name": "correlation_create_wing",
      "description": "Create a new correlation Wing in the active case. A Wing is a named correlation rule that ties feathers (artifact databases) together with a time window and a minimum-match threshold. Use ONLY when the analyst explicitly asks for a new wing, or when you have identified a correlation pattern that recurs across the case and an explicit wing would speed future analysis. ALWAYS provide reason (forensic justification) and related_evidence (concrete database:table:rowid refs that motivated the wing). The handler enforces GEP Rules 9/10/11. Returns the saved wing path and the assigned wing_id.",
      "parameters": {
        "type": "object",
        "required": [
          "wing_name",
          "proves",
          "feathers",
          "reason",
          "related_evidence"
        ],
        "properties": {
          "wing_name": {
            "type": "string",
            "description": "Human-readable wing name."
          },
          "description": {
            "type": "string"
          },
          "proves": {
            "type": "string",
            "description": "What forensic claim this wing supports (e.g. program execution, lateral movement, data staging)."
          },
          "feathers": {
            "type": "array",
            "items": {
              "type": "object",
              "required": [
                "feather_id",
                "artifact_type"
              ],
              "properties": {
                "feather_id": {
                  "type": "string"
                },
                "artifact_type": {
                  "type": "string"
                },
                "feather_database_path": {
                  "type": "string"
                },
                "weight": {
                  "type": "number",
                  "minimum": 0,
                  "maximum": 1
                },
                "tier": {
                  "type": "integer",
                  "minimum": 1,
                  "maximum": 4
                },
                "tier_name": {
                  "type": "string"
                }
              }
            },
            "description": "Feathers this wing correlates. At least one entry required."
          },
          "time_window_minutes": {
            "type": "integer",
            "default": 180,
            "description": "Correlation window in minutes. Default 180 (3 hours)."
          },
          "minimum_matches": {
            "type": "integer",
            "default": 1,
            "description": "Minimum number of feathers that must match within the window."
          },
          "anchor_priority": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Ordered artifact-type priority for anchor selection."
          },
          "semantic_rules": {
            "type": "array",
            "items": {
              "type": "object"
            },
            "description": "Optional inline semantic rules attached to this wing."
          },
          "tags": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "case_types": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Case types this wing applies to (malware, intrusion, etc.)."
          },
          "reason": {
            "type": "string",
            "description": "REQUIRED (GEP Rule 9). Forensic justification \u2014 what evidence pattern in the current case prompted this wing."
          },
          "related_evidence": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "REQUIRED (GEP Rule 10). At least one database:table:rowid reference that motivates the wing. Unresolvable refs trigger a soft warning but do not block creation."
          }
        }
      }
    },
    {
      "name": "correlation_edit_wing",
      "description": "Edit a Wing that EYE previously authored. Refuses to mutate any wing whose author is not EYE \u2014 built-in and human-authored wings stay read-only. Only the fields you supply are changed; every edit requires a fresh reason (GEP Rule 9). Returns the updated wing path and the diff list.",
      "parameters": {
        "type": "object",
        "required": [
          "wing_id",
          "reason"
        ],
        "properties": {
          "wing_id": {
            "type": "string",
            "description": "ID of the EYE-authored wing to edit."
          },
          "reason": {
            "type": "string",
            "description": "REQUIRED. Why this edit is being made."
          },
          "wing_name": {
            "type": "string"
          },
          "description": {
            "type": "string"
          },
          "proves": {
            "type": "string"
          },
          "feathers": {
            "type": "array",
            "items": {
              "type": "object"
            }
          },
          "time_window_minutes": {
            "type": "integer"
          },
          "minimum_matches": {
            "type": "integer"
          },
          "anchor_priority": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "semantic_rules": {
            "type": "array",
            "items": {
              "type": "object"
            }
          },
          "tags": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "case_types": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "related_evidence": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Optional. New evidence references supporting this edit."
          }
        }
      }
    },
    {
      "name": "correlation_create_semantic_mapping",
      "description": "Create a new Semantic Mapping or Semantic Rule in the active case. A mapping turns a technical value (EventID 4624, file pattern, process path) into a human-readable forensic meaning. Use when you spot a recurring technical artifact in the case that has clear forensic significance but is not already mapped. Use mapping_type='rule' for multi-condition AND/OR rules; otherwise 'mapping'. ALWAYS provide reason and related_evidence. The handler enforces GEP Rules 9/10/11.",
      "parameters": {
        "type": "object",
        "required": [
          "mapping_type",
          "semantic_value",
          "reason",
          "related_evidence"
        ],
        "properties": {
          "mapping_type": {
            "type": "string",
            "enum": [
              "mapping",
              "rule"
            ]
          },
          "source": {
            "type": "string",
            "description": "Artifact source name. Required for 'mapping'."
          },
          "field": {
            "type": "string",
            "description": "Column name in that source. Required for 'mapping'."
          },
          "technical_value": {
            "type": "string",
            "description": "Exact value to match. Empty when using pattern instead."
          },
          "pattern": {
            "type": "string",
            "description": "Optional regex pattern for matching."
          },
          "semantic_value": {
            "type": "string",
            "description": "Human-readable meaning."
          },
          "description": {
            "type": "string"
          },
          "category": {
            "type": "string",
            "description": "Semantic category (authentication, process_execution, etc.)."
          },
          "severity": {
            "type": "string",
            "enum": [
              "info",
              "low",
              "medium",
              "high",
              "critical"
            ],
            "default": "info"
          },
          "confidence": {
            "type": "number",
            "minimum": 0.0,
            "maximum": 1.0,
            "default": 1.0
          },
          "scope": {
            "type": "string",
            "enum": [
              "global",
              "wing",
              "pipeline"
            ],
            "default": "global"
          },
          "wing_id": {
            "type": "string",
            "description": "Required when scope is 'wing'."
          },
          "name": {
            "type": "string",
            "description": "Rule name (for mapping_type='rule')."
          },
          "conditions": {
            "type": "array",
            "items": {
              "type": "object",
              "required": [
                "feather_id",
                "field_name"
              ],
              "properties": {
                "feather_id": {
                  "type": "string"
                },
                "field_name": {
                  "type": "string"
                },
                "value": {
                  "type": "string"
                },
                "operator": {
                  "type": "string",
                  "enum": [
                    "equals",
                    "wildcard",
                    "contains",
                    "regex",
                    "greater_than",
                    "less_than"
                  ]
                }
              }
            },
            "description": "Multi-condition list for mapping_type='rule'."
          },
          "logic_operator": {
            "type": "string",
            "enum": [
              "AND",
              "OR"
            ],
            "default": "AND"
          },
          "reason": {
            "type": "string",
            "description": "REQUIRED (GEP Rule 9)."
          },
          "related_evidence": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "REQUIRED (GEP Rule 10). At least one database:table:rowid ref."
          }
        }
      }
    },
    {
      "name": "correlation_edit_semantic_mapping",
      "description": "Edit a Semantic Mapping or Rule that EYE previously authored. Refuses to mutate anything authored elsewhere (built-in or human-authored mappings remain read-only). Every edit requires a fresh reason. Returns the updated artifact path and diff list.",
      "parameters": {
        "type": "object",
        "required": [
          "artifact_id",
          "reason"
        ],
        "properties": {
          "artifact_id": {
            "type": "string",
            "description": "ID returned by correlation_create_semantic_mapping (eye_mapping_* or eye_rule_*)."
          },
          "reason": {
            "type": "string",
            "description": "REQUIRED. Why this edit is being made."
          },
          "source": {
            "type": "string"
          },
          "field": {
            "type": "string"
          },
          "technical_value": {
            "type": "string"
          },
          "pattern": {
            "type": "string"
          },
          "semantic_value": {
            "type": "string"
          },
          "description": {
            "type": "string"
          },
          "category": {
            "type": "string"
          },
          "severity": {
            "type": "string",
            "enum": [
              "info",
              "low",
              "medium",
              "high",
              "critical"
            ]
          },
          "confidence": {
            "type": "number",
            "minimum": 0.0,
            "maximum": 1.0
          },
          "name": {
            "type": "string"
          },
          "logic_operator": {
            "type": "string",
            "enum": [
              "AND",
              "OR"
            ]
          },
          "conditions": {
            "type": "array",
            "items": {
              "type": "object"
            }
          },
          "related_evidence": {
            "type": "array",
            "items": {
              "type": "string"
            }
          }
        }
      }
    }
  ],
  "initial_triage_prompt": [
    "# INITIAL INVESTIGATIVE TRIAGE PROTOCOL",
    "",
    "As the EYE Assistant, your first task upon case initialization is to generate a 'Master Forensic Triage Report' in the **Living Report Workspace**. Proactively execute the following investigative steps and **MANDATORY: Use reporting tools for every finding.**",
    "",
    "## 1. System Identity & Network Discovery",
    "- Query `registry_data.db` (table `computer_Name` or `ComputerNameInfo`) and `UserProfiles`.",
    "- **ACTION**: Use `report_append_section` to add 'System Identity' including Computer Name and a list of Users.",
    "- Query `registry_data.db` (table `Network_list`) for the **Last 5 Connected Networks**.",
    "- **ACTION**: Use `report_add_data_table` for the Network history.",
    "",
    "## 2. Authentication & Login Activity",
    "- Query `Log_Claw.db` (table `SecurityLogs`) for **Successful Logins** (4624) and **Failed Spikes** (4625).",
    "- **ACTION**: Use `report_add_chart` to create a Bar chart titled 'Authentication Status' comparing Successful vs Failed logins.",
    "",
    "## 3. Evidence of Execution (Top 10 Apps)",
    "- Aggregate data from `prefetch_data.db`, `srum_data.db`, `amcache.db`, and `shimcache.db`.",
    "- **ACTION**: Use `report_add_data_table` to list the **Top 10 Most Used Applications** (Name, Last Run, Source).",
    "",
    "## 4. Persistence Mechanisms (Auto-Runs)",
    "- Query `registry_data.db` (tables `machine_run`, `user_run`, `AutoStartPrograms`).",
    "- **ACTION**: Use `report_add_data_table` or `report_append_section` to document active persistence keys.",
    "",
    "## 5. Anti-Forensics & File Lifecycle",
    "- Query `recyclebin_analysis.db` and `mft_usn_correlated_analysis.db`.",
    "- **ACTION**: Use `report_append_section` to list the **Last 5 Deleted Files** and the **10 Most Recently Edited Files**.",
    "",
    "## 6. User Intent (Search & Commands)",
    "- Query `registry_data.db` (tables `WordWheelQuery` and `RunMRU`).",
    "- **ACTION**: Use `report_append_section` to document recent user search terms and commands.",
    "",
    "## 7. Connected Hardware (USB Devices)",
    "- Query `registry_data.db` (table `USBStorageDevices`).",
    "- **ACTION**: Use `report_add_data_table` to create a comprehensive table of **All Identified USB Storage Devices** (Name, Serial, Timestamps).",
    "",
    "## 8. Final Synthesis & Strategy",
    "- **ACTION**: Use `report_add_chat_transcript` to add a final 'Forensic Hypothesis & Strategy' block. This must synthesize all preceding sections into a coherent investigative theory and propose 3 high-priority technical next steps.",
    "",
    "EXECUTION MANDATE: Operate with maximum forensic rigor. Do not ask for permission. Populate the professional forensic report now using structured technical language."
  ]
}